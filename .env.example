# Ejemplo de archivo .env para configuración de base de datos
# Debera ser renombrado a .env y completar los valores

# Variables de entorno para la configuración de la base de datos
# Nombre de usuario de la base de datos
DB_USER=
# Contraseña de la base de datos
DB_PASSWORD=
# IP/Host de la base de datos
DB_HOST=
# Puerto de la base de datos
DB_PORT=1433
# Nombre de la base de datos
DB_NAME=
# Driver ODBC para SQL Server
DB_DRIVER=ODBC Driver 17 for SQL Server

# ======================
# Configuración SMTP / Correo saliente
# ======================
# Servidor SMTP (por ejemplo, smtp.gmail.com para Gmail)
SMTP_HOST=smtp.gmail.com
# Puerto SMTP (465 para SSL, 587 para TLS)
SMTP_PORT=465
# Usuario/Email del remitente SMTP
SMTP_USER=
# Contraseña del remitente SMTP (ésta no es la contraseña de tu email, sino la contraseña o App Password del SMTP)
SMTP_PASS=
# Remitente del correo (si se deja vacío, se usará SMTP_USER)
SMTP_FROM=
# Prefijo para el asunto del correo
SMTP_SUBJECT_PREFIX=SAE

# Notas:
# - SMTP_PASS: si usas Gmail con 2FA, crear un App Password.
# - SMTP_FROM: si se deja vacío, se usará SMTP_USER.
# - No subir tu .env real al repositorio (.gitignore debe incluirlo).
